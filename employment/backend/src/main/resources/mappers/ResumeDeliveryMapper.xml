<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gr.geias.repository.ResumeDeliveryRepository">

    <select id="findByUserId" resultType="com.gr.geias.model.ResumeDelivery">
        select
            delivery_id,
            user_id,
            resume_id,
            position_id,
            company_id,
            delivery_time,
            status,
            feedback,
            create_time,
            update_time
        from tb_resume_delivery
        where user_id = #{userId}
        order by delivery_time desc
    </select>
    
    <select id="findByCompanyId" resultType="com.gr.geias.model.ResumeDelivery">
        select
            delivery_id,
            user_id,
            resume_id,
            position_id,
            company_id,
            delivery_time,
            status,
            feedback,
            create_time,
            update_time
        from tb_resume_delivery
        where company_id = #{companyId}
        order by delivery_time desc
    </select>
    
    <select id="findByPositionId" resultType="com.gr.geias.model.ResumeDelivery">
        select
            delivery_id,
            user_id,
            resume_id,
            position_id,
            company_id,
            delivery_time,
            status,
            feedback,
            create_time,
            update_time
        from tb_resume_delivery
        where position_id = #{positionId}
        order by delivery_time desc
    </select>
    
    <select id="findByDeliveryId" resultType="com.gr.geias.model.ResumeDelivery">
        select
            delivery_id,
            user_id,
            resume_id,
            position_id,
            company_id,
            delivery_time,
            status,
            feedback,
            create_time,
            update_time
        from tb_resume_delivery
        where delivery_id = #{deliveryId}
    </select>
    
    <select id="existsByUserIdAndPositionId" resultType="java.lang.Integer">
        select count(*)
        from tb_resume_delivery
        where user_id = #{userId} and position_id = #{positionId}
    </select>
    
    <insert id="save" parameterType="com.gr.geias.model.ResumeDelivery" useGeneratedKeys="true" keyProperty="deliveryId">
        insert into tb_resume_delivery (
            user_id,
            resume_id,
            position_id,
            company_id,
            delivery_time,
            status,
            feedback,
            create_time,
            update_time
        ) values (
            #{userId},
            #{resumeId},
            #{positionId},
            #{companyId},
            #{deliveryTime},
            #{status},
            #{feedback},
            #{createTime},
            #{updateTime}
        )
    </insert>
    
    <update id="update" parameterType="com.gr.geias.model.ResumeDelivery">
        update tb_resume_delivery
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="feedback != null">feedback = #{feedback},</if>
            update_time = #{updateTime}
        </set>
        where delivery_id = #{deliveryId}
    </update>
    
    <delete id="delete">
        delete from tb_resume_delivery
        where delivery_id = #{deliveryId}
    </delete>
    
</mapper> 